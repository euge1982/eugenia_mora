//Crear un Modelo ThemeState que almacene los atributos isDark booleano y mode,
//que es un string que indica si el  tema es light o dark.

export interface ThemeState{
    isDark: boolean,
    mode: string
}
